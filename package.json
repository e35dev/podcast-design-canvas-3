{
  "name": "podcast-design-canvas",
  "version": "0.1.0",
  "private": true,
  "description": "Self-serve visual podcast production workspace — import synced speaker videos, compose with presets, preview, and export a polished episode.",
  "scripts": {
    "dev": "node scripts/dev-server.mjs",
    "preview": "node scripts/dev-server.mjs",
    "start": "node scripts/dev-server.mjs",
    "typecheck": "node scripts/typecheck.mjs",
    "lint": "node scripts/lint.mjs",
    "test": "node --test",
    "preview-build": "node scripts/preview-build.mjs"
  },
  "engines": {
    "node": ">=18"
  }
}
